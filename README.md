# Twitter-NER-Prediction-using-BERT
This folder contains CONNL-BIO formatted Twitter dataset for detecting Named Entity Recognitions. The entire app is built via BERT, a baseline version of the same prediction using BiLSTM + Softmax is also available.
